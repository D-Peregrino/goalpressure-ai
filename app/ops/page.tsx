import OpsCenter from "@/components/ops/OpsCenter";

export const metadata = {
  title: "Live OPS Center — GoalPressure AI",
  description:
    "Central operacional viva unificada: multi-view, timeline global, radar contextual e distorção de mercado.",
};

export default function OpsRoutePage() {
  return <OpsCenter />;
}
