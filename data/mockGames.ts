/**
 * @deprecated Não usar em UI — dados ao vivo vêm exclusivamente de GET /api/live-matches.
 * Mantido vazio para evitar imports acidentais em produção.
 */
import type { Match } from "@/types/domain";

export const mockGames: Match[] = [];
