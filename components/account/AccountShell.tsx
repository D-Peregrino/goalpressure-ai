"use client";

/** @deprecated Use AppShell com requireAuth — mantido como fragmento. */
export default function AccountShell({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
